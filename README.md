This is a Next.js project for managing user information with filtering and modal details.

## Getting Started

To run the project install dependencies with `npm install` or `yarn install`.

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the app.